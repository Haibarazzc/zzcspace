# Sunset Color Analyzer

Run locally:

```powershell
python -m streamlit run sunset_color_analyzer/app.py --server.port 8501
```

The app extracts a six-color palette, estimates warmth, brightness, and saturation, creates a CSS gradient, and exports a palette card and JSON data.
